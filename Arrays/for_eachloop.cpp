// for each loop is used in array or collection type of data structure In the syntax of foreach loop (x) is a declareing variable for elements in Array //

#include <iostream>
using namespace std;
int main ()
{
    int A[] = {3,5,6,7,8,9,10};
    for (int x:A)
    cout << x << endl;
    return 0;
}
